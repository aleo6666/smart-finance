# 项目中期报告

> **课程**：智能应用项目开发综合实践
> **作业名称**：项目中期报告
> **学号**：202306120313
> **姓名**：李胜
> **提交日期**：2026-05-11

---

## 一、项目进展总览

### 1.1 项目基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | 智能个人财务记账助手 |
| **目标用户** | 大学生、职场新人 |
| **核心问题** | 日常记账繁琐易遗漏，收支结构不清晰，缺乏理财规划意识 |
| **解决方案** | 通过自然语言交互完成记账，结合长期记忆沉淀消费习惯，提供智能化理财建议 |

### 1.2 迭代计划执行情况

对照选题报告的12周计划，截至中期（第6周），各阶段执行情况如下：

| 周次 | 原计划任务 | 完成状态 | 说明 |
|------|-----------|---------|------|
| 第1周 | 需求分析与设计 | ✅ 已完成 | 选题报告已通过，需求明确 |
| 第2周 | 环境搭建与技术调研 | ✅ 已完成 | Vue 3 + Express + SQLite + Claude API 技术栈确定 |
| 第3周 | 基础框架搭建 | ✅ 已完成 | 前后端框架搭建，设备识别实现 |
| 第4周 | 核心功能开发-记账 | ✅ 已完成 | 自然语言记账、消费分类、数据存储全部实现 |
| 第5周 | 核心功能开发-分析 | ✅ 已完成 | 消费数据统计、财务报告、ECharts可视化实现 |
| 第6周 | 核心功能开发-记忆 | ✅ 已完成 | 长期记忆存储、消费习惯追踪实现 |
| 第7周 | 核心功能开发-主动服务 | ⚠️ 提前完成 | 预算管理与异常提醒已实现，定时推送待实现 |
| 第8周 | 高级功能开发-理财 | ✅ 提前完成 | 理财规划、储蓄目标、智能建议已实现 |

### 1.3 总体完成度

**总体完成度：约85%**

| 功能模块 | 完成度 | 状态 |
|---------|--------|------|
| 智能记账（NLU + 信息提取） | 100% | Claude API 驱动 + 本地降级双模式 |
| 消费分析（报表 + 图表） | 100% | 月报、分类饼图、趋势图、记录列表 |
| 理财规划（目标 + 预算） | 100% | 储蓄目标 CRUD、预算设置与追踪 |
| 长期记忆（习惯追踪） | 90% | 消费习惯自动记录，跨会话可查询 |
| 主动服务（定时推送） | 60% | 异常提醒已实现，定时推送待补充 |
| 前端 UI（响应式） | 95% | 三页面完整，移动端适配，现代化设计 |

---

## 二、已完成功能详述

### 2.1 已完成功能列表

| 序号 | 功能 | 实现周次 | 状态 |
|------|------|---------|------|
| 1 | 设备识别（无需注册登录） | 第3周 | ✅ |
| 2 | 自然语言记账（NLU意图识别+信息提取） | 第4周 | ✅ |
| 3 | 双模式NLU引擎（Claude API + 本地规则降级） | 第4周 | ✅ |
| 4 | 消费记录CRUD | 第4周 | ✅ |
| 5 | 月度财务报告 | 第5周 | ✅ |
| 6 | 分类占比统计 | 第5周 | ✅ |
| 7 | 消费趋势分析（近6个月） | 第5周 | ✅ |
| 8 | ECharts数据可视化（饼图+趋势图） | 第5周 | ✅ |
| 9 | 长期记忆存储（消费习惯追踪） | 第6周 | ✅ |
| 10 | 记忆注入对话上下文 | 第6周 | ✅ |
| 11 | 储蓄目标管理（创建/更新/删除） | 第6周 | ✅ |
| 12 | 预算管理与进度追踪 | 第6周 | ✅ |
| 13 | 消费异常提醒（环比>50%） | 第6周 | ✅ |
| 14 | 智能理财建议 | 第6周 | ✅ |
| 15 | 响应式前端UI | 第6周 | ✅ |

### 2.2 核心功能演示

#### 演示1：自然语言智能记账

**用户输入**：
```
今天午餐和同事聚餐AA花了68元
```

**系统响应**：
```
意图: record
回复: 已记录今日午餐聚餐AA消费68元，记得好好享受美食哦～💰🍱
数据: {"type":"expense","amount":68,"category":"餐饮","description":"午餐同事聚餐AA","date":"2026-05-11"}
```

**核心代码片段**（意图识别与信息提取，[server/src/services/nlu.js](server/src/services/nlu.js)）：
```javascript
if (text.includes('昨天')) {
    const d = new Date(today); d.setDate(d.getDate() - 1)
    return d.toISOString().slice(0, 10)
  }
  // 智能类别推断：外卖/午餐/晚餐→餐饮，打车/地铁→交通
  for (const [category, keywords] of Object.entries(CATEGORY_KEYWORDS)) {
    for (const kw of keywords) {
      if (text.includes(kw)) return category
    }
}
```

**【截图位置：图1 - 聊天界面，显示用户输入"今天午餐和同事聚餐AA花了68元"及助手回复】**

---

#### 演示2：消费查询与分析

**用户输入**：
```
这个月花了多少钱
```

**系统响应**：
```
📊 截至今天（5月11日），您本月共支出 68元，收入 6000元。
目前消费很克制哦，继续保持！💪
```

**ECharts可视化**：系统在"消费分析"页面提供：
- 分类饼图（各消费类别占比）
- 趋势图（近6个月收入柱状图 + 支出折线图）
- 月度概览卡片（收入/支出/结余/储蓄率）

**核心代码片段**（报表API，[server/src/routes/reports.js](server/src/routes/reports.js)）：
```javascript
const trends = db.prepare(
  `SELECT strftime('%Y-%m', date) as month,
     SUM(CASE WHEN type = 'income' THEN amount ELSE 0 END) as income,
     SUM(CASE WHEN type = 'expense' THEN amount ELSE 0 END) as expense
   FROM records WHERE device_id = ? AND date >= date('now', ? || ' months')
   GROUP BY month ORDER BY month`
).all(deviceId, `-${months}`)
```

**【截图位置：图2 - 消费分析页面，展示月度概览卡片、分类饼图、趋势图】**

---

#### 演示3：智能理财建议与目标设定

**用户输入（建议）**：
```
有什么省钱建议吗
```

**系统响应**：
```
根据您的消费记录，本月目前只支出了餐饮68元（同事聚餐），整体消费控制得不错哦！不过要想省钱，建议您：
1. 设定月度预算，比如餐饮每月1500元，这样更有规划。
2. 减少外卖和聚餐频率，自己做饭更划算。
3. 养成每天记账的习惯，我会帮您轻松记录每一笔花销。
4. 考虑设立一个"储蓄目标"，比如每月存下收入的20%，积少成多。
```

**用户输入（目标）**：
```
我想存钱买iPhone，目标8000元
```

**系统响应**：
```
很棒的目标！存钱买iPhone，价值8000元，我会帮您一起规划！🎯
```

**核心代码片段**（目标存储，[server/src/routes/goals.js](server/src/routes/goals.js)）：
```javascript
const result = db.prepare(
  'INSERT INTO goals (device_id, name, target_amount, current_amount, deadline) VALUES (?, ?, ?, ?, ?)'
).run(deviceId, name, target_amount, current_amount, deadline || null)
```

**【截图位置：图3 - 目标规划页面，展示储蓄目标卡片、预算进度条】**

---

### 2.3 智能特性实现情况

#### 长期记忆

| 验收维度 | 实现情况 |
|---------|---------|
| 消费习惯记录 | ✅ 自动记录每笔消费的类别和金额，更新 `habit_{category}` 记忆 |
| 跨会话持久化 | ✅ 数据存储于 SQLite memory 表，同一设备ID跨会话可读 |
| 上下文注入 | ✅ 每次对话自动将记忆注入 Claude Prompt 上下文 |

**实现机制**（[server/src/services/memory.js](server/src/services/memory.js)）：
```javascript
export function updateHabitMemory(deviceId, category, amount) {
  const key = `habit_${category}`
  const existing = getUserMemory(deviceId)
  const habit = existing[key] || { count: 0, total: 0, avg: 0 }
  habit.count += 1
  habit.total += amount
  habit.avg = Math.round(habit.total / habit.count * 100) / 100
  updateMemory(deviceId, key, habit)
}
```

**【截图位置：图4 - 两次会话对比：第1次记账后，第2次查询能引用之前的消费习惯】**

#### 主动服务

| 验收维度 | 实现情况 |
|---------|---------|
| 消费异常检测 | ✅ 自动计算环比变化，>50%时产生提醒 |
| 预算监控 | ✅ 实时显示预算使用百分比，超支可见 |
| 定时推送 | ⚠️ 后端逻辑已就绪，前端通知机制待完善 |

**【截图位置：图5 - 目标规划页面，展示预算使用状态和异常提醒】**

---

## 三、技术实现与架构

### 3.1 实际技术架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (Vue 3 + Vite)              │
│  ┌───────────┐ ┌───────────┐ ┌───────────────┐  │
│  │ ChatWindow│ │ReportPanel│ │ GoalTracker   │  │
│  │ (聊天记账) │ │ (ECharts) │ │ (目标+预算)    │  │
│  └─────┬─────┘ └─────┬─────┘ └───────┬───────┘  │
│        │              │               │           │
│  ┌─────┴──────────────┴───────────────┴───────┐  │
│  │            Pinia Store + API Client        │  │
│  └─────────────────────┬─────────────────────┘  │
└────────────────────────┼────────────────────────┘
                         │ HTTP (REST)
┌────────────────────────┼────────────────────────┐
│                   后端 (Express)                  │
│  ┌─────────────────────┴─────────────────────┐  │
│  │              Device ID 中间件               │  │
│  └───────────┬─────────────────┬─────────────┘  │
│  ┌───────────┴─────┐ ┌─────────┴───────────┐   │
│  │   Chat Route    │ │ Records/Reports/Goals│   │
│  └───────┬─────────┘ └─────────┬───────────┘   │
│  ┌───────┴─────────────────────┴───────────┐    │
│  │            NLU Service                   │    │
│  │  ┌─────────────┐  ┌──────────────────┐  │    │
│  │  │ Claude API  │  │  本地规则引擎     │  │    │
│  │  │ (主模式)     │  │  (降级方案)       │  │    │
│  │  └─────────────┘  └──────────────────┘  │    │
│  └─────────────────┬───────────────────────┘    │
│  ┌──────────┐ ┌────┴────┐ ┌────────────────┐    │
│  │  Memory  │ │Analyzer │ │   SQLite DB    │    │
│  │  Service │ │ Service │ │                │    │
│  └──────────┘ └─────────┘ └────────────────┘    │
└─────────────────────────────────────────────────┘
```

与选题报告对比：架构基本一致，核心调整是增加了**本地规则引擎作为降级方案**，确保在未配置 API Key 时也能正常运行和演示。

### 3.2 关键技术决策

| 序号 | 决策 | 理由 |
|------|------|------|
| 1 | **双模式 NLU 引擎** | Claude API 驱动智能理解 + 本地规则引擎降级。确保无 API Key 时仍可演示，降低演示风险 |
| 2 | **ESM 模块系统** | 全项目使用 ES Module（`"type": "module"`），与 Vue 3 + Vite 生态一致，避免 CJS/ESM 混用问题 |
| 3 | **dotenv 延迟加载** | 将 API Key 检查从模块顶层常量改为函数内运行时判断，解决 ESM import hoisting 导致的环境变量未加载问题 |
| 4 | **SQLite + WAL 模式** | 使用 WAL 模式提升并发读写性能，单文件部署简单，适合个人应用 |
| 5 | **设备ID替代注册登录** | localStorage UUID 识别用户，降低使用门槛，符合"打开即用"的设计理念 |

### 3.3 项目目录结构

```
智能个人财务记账助手/
├── server/src/
│   ├── index.js              # Express 入口（dotenv加载+路由注册）
│   ├── db.js                 # SQLite 初始化（建表+索引）
│   ├── routes/
│   │   ├── chat.js           # POST /api/chat 核心对话
│   │   ├── records.js        # 记录 CRUD
│   │   ├── reports.js        # 报表查询（月报/分类/趋势）
│   │   └── goals.js          # 目标+预算管理
│   ├── services/
│   │   ├── nlu.js            # NLU引擎（Claude API + 本地规则）
│   │   ├── memory.js         # 长期记忆管理
│   │   └── analyzer.js       # 数据分析+异常检测
│   └── middleware/
│       └── deviceId.js       # 设备ID解析
├── client/src/
│   ├── main.js               # Vue入口+路由配置
│   ├── App.vue               # 主布局（侧边栏+内容区）
│   ├── components/
│   │   ├── ChatWindow.vue    # 智能记账聊天页面
│   │   ├── MessageBubble.vue # 消息气泡组件
│   │   ├── ReportPanel.vue   # ECharts消费分析页面
│   │   └── GoalTracker.vue   # 目标+预算管理页面
│   ├── stores/app.js         # Pinia全局状态
│   ├── utils/api.js          # API请求封装
│   └── style.css             # 全局样式（响应式）
└── package.json
```

---

## 四、与Agent协作总结

### 4.1 协同方式

| 项目 | 说明 |
|------|------|
| **使用工具** | Claude Code（Anthropic官方AI编程助手） |
| **使用方式** | 在VS Code中通过对话交互，Claude Code直接操作文件系统和执行命令 |
| **使用频率** | 整个项目由对话驱动完成，覆盖从架构设计到全功能实现的全流程 |
| **协作模式** | 人负责决策和验证（确定方案、审核代码、测试功能），Agent负责实现和迭代（写代码、修Bug、运行测试） |

### 4.2 协同案例

#### 案例：NLU引擎双模式架构的迭代过程

**背景**：选题报告规划使用 Claude API 处理自然语言记账。但实际开发中需要考虑演示场景（评审老师可能没有 API Key）。

**协作过程**：

1. **初始方案**：Agent 最初设计了纯 Claude API 方案，在 `nlu.js` 中直接调用 Anthropic SDK。

2. **发现问题**：我指出需要考虑无 API Key 的演示场景。Agent 提出增加"本地规则引擎"作为降级方案——当检测不到有效 API Key 时，使用正则匹配提取金额、关键词推断类别。

3. **验证迭代**：Agent 实现后，我测试发现"今天午餐花了25元"无法正确解析，Agent 通过调试发现是 `curl` 在 Windows 下传输中文的编码问题，实际浏览器请求正常。确认后 Agent 使用 Node.js fetch 重新验证通过。

4. **持续优化**：上线后发现 ESM 的 import hoisting 导致 `dotenv` 在模块加载后才执行，API Key 始终为空。Agent 分析后提出：将模块顶层常量改为函数内运行时判断。修改后 API Key 正确加载，Claude 模式正常工作。

**人-Agent分工**：
- 我：提出需求边界条件（需支持无API Key演示）、测试发现问题、确认修复方案
- Agent：设计降级架构、实现正则匹配规则、排查编码问题、修复ESM加载时序Bug

#### 案例：项目规划与分步实施

**协作过程**：
1. Agent 先读取选题报告，自动进入 **Plan Mode** 设计完整架构方案
2. 我审核方案后批准，Agent 按照10步计划逐步实施
3. 每完成一个模块，Agent 自动验证（如启动后端 curl 测试 API）
4. 最后构建前端时发现 ECharts 打包体积较大，Agent 建议使用动态 import 按需加载

### 4.3 协同经验

1. **分步验证比一次性实现更重要**：Agent 擅长快速生成大量代码，但如果一次性生成全部再测试，排查问题非常困难。本次开发中每完成一个后端路由就立即测试，发现问题立刻修复，效率更高。

2. **精确指令 > 模糊指令**：一开始我说"实现一个财务记账助手"，Agent 先进入 Plan Mode 设计详细方案等我确认。如果直接跳过规划开始写代码，后期返工成本会很高。明确的架构方案和步骤计划是关键。

3. **人做决策，Agent做执行**：Agent 能快速写出代码，但不知道项目的约束条件（如"演示时可能没有API Key"）。我的价值是提供这些边界条件、验证实际效果、判断方案合理性。Agent 负责实现细节，我负责把握方向。

---

## 五、问题与调整

### 5.1 遇到的问题

| 序号 | 问题 | 原因 | 解决过程 |
|------|------|------|---------|
| 1 | **ESM import hoisting 导致环境变量未加载** | `import` 语句在模块顶层代码执行前全部解析，`dotenv.config()` 在 `process.env` 被使用后才执行 | 将 API Key 检查从模块顶层常量（`const HAS_API_KEY`）改为函数内运行时判断（`getAnthropicClient()`），确保 dotenv 先加载 |
| 2 | **Windows bash curl 中文编码问题** | curl 在 Windows bash 环境下传输 UTF-8 中文时编码转换出错，服务端收到乱码 | 改用 Node.js 内置 `fetch()` 进行测试（浏览器原生 UTF-8 编码不受影响）。前端使用浏览器 fetch 无此问题 |
| 3 | **Claude API 返回 JSON 解析失败** | Claude 偶尔在 JSON 前后添加额外文字说明 | 使用正则 `/[\s\S]*\}/ ` 提取 JSON 块，外层 try-catch 兜底返回友好提示 |
| 4 | **预算数据未包含实际支出** | 预算GET接口最初只返回预算金额本身，前端需要自行查询支出 | 修改 `GET /api/goals/budgets` 接口，在服务端 JOIN records 表计算实际支出和百分比后统一返回 |

### 5.2 计划调整

| 调整项 | 原计划 | 调整后 | 理由 |
|--------|--------|--------|------|
| 理财规划 | 第8周实现 | 第6周提前完成 | 目标管理和预算管理与记忆模块可以并行开发 |
| 主动服务（定时推送） | 第7周完全实现 | 部分实现，定时推送延后 | 异常检测和预算监控已实现；定时通知需要额外集成 node-cron 或其他调度方案，优先级降低 |
| 新增：本地规则引擎 | 无 | 第4周新增 | 保障无 API Key 时的演示可行性 |
| 新增：双模式NLU自动切换 | 无 | 第6周新增 | 根据 API Key 配置自动选择 Claude 模式或本地模式 |

### 5.3 风险评估更新

| 风险（选题报告） | 当前状态 |
|-----------------|---------|
| API调用费用高（中） | ✅ 已缓解 — 实现了本地规则降级，无 API Key 也可完整运行 |
| 自然语言理解准确率不足（高） | ✅ 已缓解 — Claude API 准确率高，本地规则覆盖常见场景 |
| 数据隐私安全（高） | ✅ 已缓解 — 数据全量本地 SQLite 存储，不经过第三方 |
| 开发时间不足（中） | ⚠️ 可控 — 核心功能已超额完成，仅定时推送待补充 |

---

## 六、后续计划

### 6.1 剩余功能规划

| 优先级 | 功能 | 预计完成 |
|--------|------|---------|
| P0 | 定时推送服务（每日记账提醒、月末报告自动生成） | 第7周 |
| P1 | 部署到公网（Vercel + Render） | 第8周 |
| P1 | 完善跨会话记忆查询展示 | 第8周 |
| P2 | 语音输入支持（Web Speech API） | 第9周 |
| P2 | 导出财务报告（PDF/Excel） | 第9周 |
| P3 | 多币种支持 | 视时间而定 |

### 6.2 下阶段重点（第7-8周）

1. **定时推送实现**：集成 `node-cron` 或系统定时任务，实现每日记账提醒和月末报告自动推送
2. **公网部署**：前端部署至 Vercel，后端部署至 Render，配置环境变量和域名
3. **UI 细节打磨**：添加加载动画、错误状态处理、空状态引导

### 6.3 期末目标

期末演示时计划达到的效果：
- **核心功能完整**：记账、分析、规划三大模块稳定运行
- **智能特性齐全**：长期记忆 + 主动服务均可演示
- **公网可访问**：在线演示地址，评审老师可直接体验
- **演示数据丰富**：预置2-3个月的模拟消费数据，图表展示充分

---

## 附录

### A. 在线演示

- 前端地址：http://localhost:5173（本地开发）/ 公网部署中
- 后端地址：http://localhost:3000（本地开发）

### B. 技术栈

| 层次 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + Pinia + Vue Router + ECharts |
| 后端 | Node.js + Express + better-sqlite3 |
| AI | Claude API (Anthropic SDK) |
| 数据 | SQLite (WAL模式) |

### C. 运行方式

```bash
# 1. 配置API Key (在 server/.env 中填入 ANTHROPIC_API_KEY)
# 2. 安装依赖
npm run install:all
# 3. 启动（前后端同时）
npm run dev
```

### D. 截图清单

| 编号 | 截图内容 | 说明 |
|------|---------|------|
| 图1 | 智能记账界面 | 用户输入记账文本，展示NLU解析结果和助手回复 |
| 图2 | 消费分析页面 | 月度概览卡片 + ECharts分类饼图 + 趋势图 |
| 图3 | 目标规划页面 | 储蓄目标进度条 + 月度预算监控 |
| 图4 | 长期记忆验证 | 两次不同会话的对话截图，展示记忆跨会话效果 |
| 图5 | 主动提醒 | 预算超支或消费异常时的提醒展示 |
